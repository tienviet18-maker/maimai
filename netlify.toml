# Legacy Netlify config (migrated to Cloudflare Pages).
# Prefer: npm run build → publish dist/ via wrangler.toml
[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
